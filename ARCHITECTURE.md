# << 恐怖大怪獸 >> 遊戲架構總結（供下一個遊戲沿用）

- 整理日期：2026-09-12
- 目前版本：v3.36
- 上線網址：https://phoenix0780-crypto.github.io/yuna-math/
- Repo：phoenix0780-crypto/yuna-math（只有一個 index.html，無建置流程）
- 目標使用者：Yuna（小一）、全家；核心目的是**培養數感**，不是考倒小孩
- 主要裝置：iPhone Safari（直式為主）、電腦瀏覽器

---

## 1. 技術架構

### 1.1 單一檔案
- 全部在 `index.html`：CSS + HTML + JS，約 1,100 行
- 外部依賴只有兩個 CDN：Three.js r128（3D 寶石）、Google Fonts（Fredoka + Noto Sans TC）
- 沒有後端、沒有 build、沒有框架；部署 = 覆蓋 index.html

### 1.2 資料儲存（localStorage）
- 封裝成 `store.get/set/del`，localStorage 不可用時退回記憶體物件（Claude 預覽、無痕模式也能跑）
- key 命名：
  - `progress_<playerId>`、`lastLevel_<playerId>`：每位玩家獨立進度
  - `board`：排行榜（全家共用，最多 50 筆）
  - `player`、`player2`、`guestName`：玩家選擇
  - `mode`、`mat`、`pal`、`clearSfx`、`bgm`、`speed`、`angel`、`diag`、`vib`、`shake`、`shake2`、`gl`：偏好設定（共用）
  - `tutorialDone`、`pickedOnce`：一次性引導旗標
  - `crashCount`、`booting`：崩潰保護
- 舊資料遷移：偵測到舊 key（如 `progress`）就搬到新 key，不讓玩家進度消失

### 1.3 畫面切換
- 四個 `.screen`：`s-title`（首頁）、`s-game`、`s-settings`、`s-board`（排行榜）
- `show(id)` 統一切換，同時控制 ⚙️ 齒輪顯示與 `body.home`（首頁用亮背景、遊戲用暗背景）
- 設定頁記住來源畫面，返回時接續（遊戲中開設定會暫停）

### 1.4 版面策略（最重要的踩雷區）
- 遊戲頁用 CSS Grid 命名區域：`hud / track / grid / diff / tool`，直式一欄、橫式（高度 < 600px）兩欄
- **方塊大小由 JS 計算**（`fitGrid`）：視窗高度 − 各區塊實際高度 → 剩餘給格子；不要用 `getBoundingClientRect().top` 反推，版面溢出時會惡性循環
- 3D 畫布貼在格子底下，位置用 `offsetLeft/offsetTop`（不受 transform/動畫影響），不要用 `getBoundingClientRect`
- 進關卡後多次重算（字型載入、rAF、120ms、600ms）確保完整呈現
- 教學區用固定高度容器 + opacity 切換內容，**絕不能讓元素出現/消失改變高度**（小孩看了會不舒服）
- 安全區：`env(safe-area-inset-*)`、`viewport-fit=cover`
- 所有尺寸用 `min(clamp(...), Xvh)` 同時受寬與高限制

### 1.5 3D 寶石（Three.js）
- 正交相機、一張 alpha 畫布、DOM 方塊只留數字（透明）當觸控目標
- **物件池**：寶石物件只建一次，之後更新位置/幾何/材質；每次重建會讓 iOS Safari 記憶體爆掉整頁崩潰
- 幾何：2D 輪廓 → 桌面/冠部/亭部三層自建 BufferGeometry（心形、圓、枕形、四葉、風箏、星、方、梨、馬眼）
- 材質 7 種、配色 6 套，皆可在設定切換即時套用
- 崩潰保護：載入記旗標 8 秒後清除，連續 2 次未清除 → 自動退回 CSS 寶石
- iOS：抗鋸齒開、像素密度上限 2、`powerPreference:'low-power'`

### 1.6 音訊（全部程式合成，無音檔）
- Web Audio 合成：音效 10 種（噪音 + 振盪器）、背景音樂（卡農 D 大調和弦 + 旋律排程）
- iOS 靜音開關會讓 Web Audio 無聲 → 第一次觸控時循環播放一段靜音 `<audio>` 把音訊切到媒體類別
- 第一次觸控才啟動 AudioContext（瀏覽器限制）
- 切到背景自動停音樂、暫停遊戲

### 1.7 觸控
- 用 Pointer Events 統一手機/滑鼠；支援點兩下或滑動
- 命中判定：先 `elementFromPoint`，落空時用座標反推最接近的格子（3D 寶石會超出格子）
- `touch-action:manipulation`、`overscroll-behavior:none`、`user-select:none` 防止縮放/下拉/選字
- iOS 網頁**無法震動**，改用畫面震動（screen shake）當回饋

---

## 2. 遊戲設計架構

### 2.1 三種模式（首頁切換，預設數感）
| 模式 | 目的 | 盤面 | 壓力 | 特色 |
|---|---|---|---|---|
| 🌱 數感 | 建立 10 的分合 | 5×6 固定 | 無 | 圓點區、每顆旁邊必有 1~2 個搭檔、錯了說明不懲罰 |
| 🦖 冒險 | 熟練後的挑戰 | 6×7 → 7×9 | 暴龍追公主 | 重力下落、連擊、公主大絕、Boss 關 |
| 👫 親子 | 兩人互動 | 5×6 固定 | 無 | 輪流出題/找搭檔、各自計分 |

### 2.2 核心規則
- 相鄰（含斜向）兩顆加起來 = 10 就消除
- 5 只能配 5，全關減半且有全盤上限（避免 5 群聚）
- 洗牌袋生成：1/9、2/8、3/7、4/6 各兩組一袋，長期平均
- 數感/親子盤面用「兩兩相鄰配對」鋪滿，消除後**原地補一對**不下落，並修正到每顆 1~2 個搭檔

### 2.3 數感回饋設計（可直接沿用到下一個遊戲）
- **圓點區**：上排 10 格有框（10 的邊界），下排 10 格虛線；超過 10 從右往左亮
- 圓點顏色 = 寶石顏色，第一顆與第二顆各自同色
- 訊息區固定兩行高、大字（20~32px）
- 錯誤時說「7 + 6 = 13，超出 3」或「還差 3」，不扣分
- 結算顯示「今天比較卡的組合」（錯誤配對統計）
- 天使唸算式「對！3 加 7 是 10」

### 2.4 難度與情緒調控（給 6 歲）
- 暴龍速度預設「更慢」，五段可調
- 天使提示五段（超級常～關閉），預設「很常」
- 失敗畫面不出現「失敗」字樣，天使遞重玩鍵
- 怪獸可愛版（暴龍有牙但圓眼腮紅）

### 2.5 玩家系統
- 八位固定角色（各有 SVG 頭像）+ 一位可改名
- 每位獨立進度（解鎖、星星、上次關卡），排行榜共用，偏好設定共用
- 親子模式同一區指引選第一位/第二位（右上角數字徽章）
- 冒險模式被追的主角 = 目前玩家

### 2.6 排行榜
- 記錄：玩家、關卡、模式、分數、星星/結果（過關/被抓/中離）、日期時間
- 中離與失敗也記（消過至少一對才記）

### 2.7 首頁資訊架構
標題 → 一句玩法 → 模式三鈕 → 模式說明（含建議年級）→ 誰在玩（收合膠囊）→ 關卡列 → 排行榜
- 第一次玩：第一關發光 + 文字指引；之後：上次關卡發光、無文字
- 續玩不用額外按鈕

### 2.8 設定頁
- 依模式顯示（冒險專屬：暴龍速度、斜向、消除/連擊畫面震動）
- 分類：聲音（音樂/音效+試聽）、畫面（3D/材質/配色/震動）、難度（速度/天使）、資料（重置該玩家進度）
- DEV-UNLOCK 測試開關（release 前移除，程式中三處有標記）

---

## 3. 視覺風格
- 深色星空 + 淡星雲 + 半顆地球；首頁亮一級（`body.home`），遊戲中暗
- 字型：Fredoka（數字）+ Noto Sans TC（中文），粗體為主
- 主色：金 `#ffd166`（強調/選取）、奶白 `#fff5e1`（文字）、深紫 `#2a1a3c`（按鈕字）
- 按鈕：圓角膠囊、底部 4~6px 實色陰影、按下位移
- 角色頭像一律圓形 + 4px 描邊，全部原創 SVG（不用任何 IP）
- 寶石配色：色相間距 ≥ 22°、9 是白色、7 紫 8 藍不相鄰撞色

---

## 4. 開發流程與踩雷規則

### 4.1 修改流程
1. 用 Python 字串替換修改 index.html（`assert s.count(a)==1` 再 replace）
2. `node -e "new Function(js)"` 檢查語法
3. jsdom 模擬：startLevel → countMoves → tap → 驗證狀態
4. GitHub Contents API 推送（需 fine-grained PAT：Contents + Administration RW）
5. 版本號集中在 `const VERSION`，全頁左下角顯示

### 4.2 已踩過的雷（下次要避免）
- **Python 腳本中 `.replace('*','')` 會吃掉 CSS 的乘號與 `> *` 選擇器** → 多條 CSS 無效造成版面跳動；替換後要 grep 確認
- **rep 中途 assert 失敗時檔案不會寫入，但後面的 push 仍會執行** → 推上舊版；push 前先確認版本號有變
- 用 index 切除程式區塊時會誤刪相鄰函式（如 `stamp`、`needAdj`）→ 刪除後跑完整流程測試（過關、失敗、回首頁）
- 每次重建 Three.js 物件 → iOS 崩潰；一定用物件池
- `getBoundingClientRect` 在動畫中取值會偏移
- 固定 `max-height` 的抽屜會切掉內容 → 用 `grid-template-rows: 0fr → 1fr`
- `<label>` 包按鈕在手機會轉發點擊 → 用 `<div>`
- Google Fonts / cdnjs 在容器內無法測試，3D 畫面只能實機驗證
- GitHub Pages 有 10 分鐘 CDN 快取，測試用 `?v=xxx`
- iOS LINE 無法傳 .html，只能分享網址
- 遊戲邏輯與提示要一致：不能讓玩家「算對卻找不到」（v3.10 的教訓 → 改為保證搭檔存在）

### 4.3 未做／可延伸
- 跨裝置排行榜（需 GAS 後端）
- 真實鋼琴取樣的背景音樂
- 湊 5 / 湊 20 的進階模式
- 語音唸題（Web Speech API）
- 每日目標與家長統計頁

---

## 5. 檔案
- 現行：`/yuna-math-game/index.html`（v3.36）
- 備份：`index_v15_css.html`（純 CSS 寶石版）、`index_v250_backup.html`（三模式之前）
- 本文件：`claude_恐怖大怪獸遊戲架構_20260912.md`
